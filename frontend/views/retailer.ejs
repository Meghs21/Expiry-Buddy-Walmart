<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upload Product - ExpiryBuddy</title>
    <script src="https://cdn.tailwindcss.com/3.4.16"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: { primary: "#10b981", secondary: "#f59e0b" },
            borderRadius: {
              none: "0px",
              sm: "4px",
              DEFAULT: "8px",
              md: "12px",
              lg: "16px",
              xl: "20px",
              "2xl": "24px",
              "3xl": "32px",
              full: "9999px",
              button: "8px",
            },
          },
        },
      };
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Pacifico&family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css"
    />
    <style>
      :where([class^="ri-"])::before { content: "\f3c2"; }
      body {
          font-family: 'Inter', sans-serif;
      }
      .custom-checkbox {
          appearance: none;
          width: 1.25rem;
          height: 1.25rem;
          border: 2px solid #d1d5db;
          border-radius: 4px;
          background-color: white;
          cursor: pointer;
          position: relative;
      }
      .custom-checkbox:checked {
          background-color: #10b981;
          border-color: #10b981;
      }
      .custom-checkbox:checked::after {
          content: '';
          position: absolute;
          left: 6px;
          top: 2px;
          width: 5px;
          height: 10px;
          border: solid white;
          border-width: 0 2px 2px 0;
          transform: rotate(45deg);
      }
      .custom-checkbox:focus {
          outline: 2px solid #10b981;
          outline-offset: 2px;
      }
      input:not([type="checkbox"]), textarea, select {
          border: 1px solid #e5e7eb;
          transition: all 0.2s;
      }
      input:not([type="checkbox"]):focus, textarea:focus, select:focus {
          border-color: #10b981;
          box-shadow: 0 0 0 2px rgba(16, 185, 129, 0.1);
          outline: none;
      }
      input[type="number"]::-webkit-inner-spin-button,
      input[type="number"]::-webkit-outer-spin-button {
          -webkit-appearance: none;
          margin: 0;
      }
      input[type="number"] {
          -moz-appearance: textfield;
      }
    </style>
  </head>
  <body class="bg-gray-50 min-h-screen">
    <div class="max-w-6xl mx-auto px-4 py-8">
      <!-- Header Section -->
      <header class="mb-10">
        <div class="flex flex-col md:flex-row items-center justify-between">
          <div class="mb-6 md:mb-0">
            <h1 class="font-['Pacifico'] text-4xl text-primary mb-3">
              ExpiryBuddy
            </h1>
            <p class="text-xl md:text-2xl text-gray-700 max-w-xl font-light">
              Give products a second chance. Help your community. Save the
              planet.
            </p>
          </div>
          <div
            class="w-full md:w-1/3 h-48 bg-contain bg-no-repeat bg-center"
            style="background-image: url('https://readdy.ai/api/search-image?query=Eco-friendly%20illustration%20with%20green%20leaves%20and%20recycling%20symbols%2C%20sustainability%20concept%20art%2C%20minimalist%20design%20with%20soft%20green%20tones%2C%20environmental%20conservation%20theme%2C%20clean%20and%20modern%20aesthetic%2C%20digital%20artwork&width=400&height=300&seq=1&orientation=landscape');"
          ></div>
        </div>
      </header>

      <!-- Info Cards -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
        <div
          class="bg-green-50 rounded-lg p-5 shadow-sm flex flex-col items-center text-center"
        >
          <div
            class="w-12 h-12 flex items-center justify-center text-primary mb-3"
          >
            <i class="ri-time-line ri-2x"></i>
          </div>
          <h3 class="font-semibold text-lg mb-2">Quick Upload, Under 2 Mins</h3>
          <p class="text-gray-600">
            Our streamlined process makes it easy to list your products quickly
            and efficiently.
          </p>
        </div>
        <div
          class="bg-green-50 rounded-lg p-5 shadow-sm flex flex-col items-center text-center"
        >
          <div
            class="w-12 h-12 flex items-center justify-center text-primary mb-3"
          >
            <i class="ri-exchange-dollar-line ri-2x"></i>
          </div>
          <h3 class="font-semibold text-lg mb-2">
            Recover Losses via Smart Discounts
          </h3>
          <p class="text-gray-600">
            Turn potential waste into revenue with our intelligent pricing
            suggestions.
          </p>
        </div>
        <div
          class="bg-green-50 rounded-lg p-5 shadow-sm flex flex-col items-center text-center"
        >
          <div
            class="w-12 h-12 flex items-center justify-center text-primary mb-3"
          >
            <i class="ri-earth-line ri-2x"></i>
          </div>
          <h3 class="font-semibold text-lg mb-2">Support a Circular Economy</h3>
          <p class="text-gray-600">
            Join our community of eco-conscious retailers making a difference
            every day.
          </p>
        </div>
      </div>

      
        <!-- <form id="product-form"  action="/products" method="POST">
          <div class="mb-8">
            <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">
              Basic Information
            </h3>

            <div class="mb-5">
              <label for="product-name" class="block text-gray-700 mb-2"
                >Product Name</label
              >
              <input
                type="text"
                id="product-name"
                class="w-full px-4 py-3 rounded bg-gray-50"
                placeholder="Enter product name"
                name="name"
              />
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-5">
              <div>
                <label for="category" class="block text-gray-700 mb-2"
                  >Category</label
                >
                <div class="relative">
                  <select
                    id="category"
                    class="w-full px-4 py-3 rounded bg-gray-50 appearance-none pr-8"
                  >
                    <option value="" disabled selected>Select category</option>
                    <option value="dairy">Dairy</option>
                    <option value="bakery">Bakery</option>
                    <option value="produce">Produce</option>
                    <option value="meat">Meat & Seafood</option>
                    <option value="pantry">Pantry Items</option>
                    <option value="frozen">Frozen Foods</option>
                    <option value="beverages">Beverages</option>
                  </select>
                  <div
                    class="absolute right-3 top-1/2 transform -translate-y-1/2 pointer-events-none w-5 h-5 flex items-center justify-center"
                  >
                    <i class="ri-arrow-down-s-line"></i>
                  </div>
                </div>
              </div>
              <div>
                <label for="subcategory" class="block text-gray-700 mb-2"
                  >Subcategory</label
                >
                <div class="relative">
                  <select
                    id="subcategory"
                    class="w-full px-4 py-3 rounded bg-gray-50 appearance-none pr-8"
                  >
                    <option value="" disabled selected>
                      Select subcategory
                    </option>
                    <option value="milk">Milk & Cream</option>
                    <option value="cheese">Cheese</option>
                    <option value="yogurt">Yogurt</option>
                    <option value="bread">Bread</option>
                    <option value="pastries">Pastries</option>
                  </select>
                  <div
                    class="absolute right-3 top-1/2 transform -translate-y-1/2 pointer-events-none w-5 h-5 flex items-center justify-center"
                  >
                    <i class="ri-arrow-down-s-line"></i>
                  </div>
                </div>
              </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="original-price" class="block text-gray-700 mb-2"
                  >Original Price ($)</label
                >
                <input
                  type="number"
                  id="original-price"
                  class="w-full px-4 py-3 rounded bg-gray-50"
                  placeholder="0.00"
                  min="0"
                  step="0.01"
                  name="price"
                />
              </div>
              <div>
                <label for="discount" class="block text-gray-700 mb-2"
                  >Discount (%)</label
                >
                <input
                  type="number"
                  id="discount"
                  class="w-full px-4 py-3 rounded bg-gray-50"
                  placeholder="0"
                  min="0"
                  max="100"
                  name="discount"
                />
              </div>
            </div>
          </div>

          
          <div class="mb-8">
            <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">
              Product Details
            </h3>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-5">
              <div>
                <label for="quantity" class="block text-gray-700 mb-2"
                  >Quantity</label
                >
                <input
                  type="number"
                  id="quantity"
                  class="w-full px-4 py-3 rounded bg-gray-50"
                  placeholder="0"
                  min="1"
                  name="quantity"
                />
              </div>
              <div>
                <label for="expiry-date" class="block text-gray-700 mb-2"
                  >Expiry Date</label
                >
                <input
                  type="date"
                  id="expiry-date"
                  class="w-full px-4 py-3 rounded bg-gray-50"
                  name="expiryDate"
                />
              </div>
            </div>

            <div class="mb-5">
              <label for="store-location" class="block text-gray-700 mb-2"
                >Store Location</label
              >
              <input
                type="text"
                id="store-location"
                class="w-full px-4 py-3 rounded bg-gray-50"
                placeholder="Enter store address"
                name="location"
              />
            </div>

            <div class="mb-5">
              <label for="description" class="block text-gray-700 mb-2"
                >Product Description</label
              >
              <textarea
                id="description"
                rows="4"
                class="w-full px-4 py-3 rounded bg-gray-50"
                placeholder="Describe your product..."
              ></textarea>
            </div>

            <div>
              <label for="image-url" class="block text-gray-700 mb-2"
                >Image URL</label
              >
              <input
                name="imageUrl"
                type="url"
                id="image-url"
                class="w-full px-4 py-3 rounded bg-gray-50"
                placeholder="https://..."
              />
            </div>
          </div>

          <div class="mb-8">
            <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">
              Additional Options
            </h3>

            <div class="flex items-start mb-4">
              <input
                name="is_perishable"
                type="checkbox"
                id="perishable"
                class="custom-checkbox mt-1"
              />
              <label for="perishable" class="ml-3 text-gray-700"
                >This is a perishable item</label
              >
            </div>

            <div class="flex items-start">
              <input type="checkbox" id="donate" class="custom-checkbox mt-1" />
              <label for="donate" class="ml-3 text-gray-700"
                >I agree to donate unsold items to local shelters</label
              >
            </div>
          </div> -->
          <!-- Form Section -->
        <div class="bg-white rounded-xl shadow-md p-6 mb-10">
            <h2 class="text-2xl font-semibold mb-6 text-gray-800">
               Upload Your Product
             </h2>

          <form id="product-form" action="/products" method="POST" enctype="application/x-www-form-urlencoded">
           <!-- Basic Info Section -->
            <div class="mb-8">
             <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">
               Basic Information
            </h3>

    <div class="mb-5">
      <label for="product-name" class="block text-gray-700 mb-2">Product Name</label>
      <input
        type="text"
        id="product-name"
        name="name"
        class="w-full px-4 py-3 rounded bg-gray-50"
        placeholder="Enter product name"
        required
      />
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-5">
      <div>
        <label for="category" class="block text-gray-700 mb-2">Category</label>
        <select
          id="category"
          name="category"
          class="w-full px-4 py-3 rounded bg-gray-50 appearance-none pr-8"
          required
        >
          <option value="" disabled selected>Select category</option>
          <option value="dairy">Dairy</option>
          <option value="bakery">Bakery</option>
          <option value="produce">Produce</option>
          <option value="meat">Meat & Seafood</option>
          <option value="pantry">Pantry Items</option>
          <option value="frozen">Frozen Foods</option>
          <option value="beverages">Beverages</option>
        </select>
      </div>
      <div>
        <label for="subcategory" class="block text-gray-700 mb-2">Subcategory</label>
        <select
          id="subcategory"
          name="subcategory"
          class="w-full px-4 py-3 rounded bg-gray-50 appearance-none pr-8"
        >
          <option value="" disabled selected>Select subcategory</option>
          <option value="milk">Milk & Cream</option>
          <option value="cheese">Cheese</option>
          <option value="yogurt">Yogurt</option>
          <option value="bread">Bread</option>
          <option value="pastries">Pastries</option>
        </select>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <label for="original-price" class="block text-gray-700 mb-2">Original Price ($)</label>
        <input
          type="number"
          id="original-price"
          name="price"
          class="w-full px-4 py-3 rounded bg-gray-50"
          placeholder="0.00"
          min="0"
          step="0.01"
          required
        />
      </div>
      <div>
        <label for="discount" class="block text-gray-700 mb-2">Discount (%)</label>
        <input
          type="number"
          id="discount"
          name="discount"
          class="w-full px-4 py-3 rounded bg-gray-50"
          placeholder="0"
          min="0"
          max="100"
        />
      </div>
    </div>
  </div>

  <!-- Details Section -->
  <div class="mb-8">
    <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">Product Details</h3>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-5">
      <div>
        <label for="quantity" class="block text-gray-700 mb-2">Quantity</label>
        <input
          type="number"
          id="quantity"
          name="quantity"
          class="w-full px-4 py-3 rounded bg-gray-50"
          placeholder="0"
          min="1"
          required
        />
      </div>
      <div>
        <label for="expiry-date" class="block text-gray-700 mb-2">Expiry Date</label>
        <input
          type="date"
          id="expiry-date"
          name="expiryDate"
          class="w-full px-4 py-3 rounded bg-gray-50"
          required
        />
      </div>
    </div>

    <div class="mb-5">
      <label for="store-location" class="block text-gray-700 mb-2">Store Location</label>
      <input
        type="text"
        id="store-location"
        name="location"
        class="w-full px-4 py-3 rounded bg-gray-50"
        placeholder="Enter store address"
        required
      />
    </div>

    <div class="mb-5">
      <label for="description" class="block text-gray-700 mb-2">Product Description</label>
      <textarea
        id="description"
        name="description"
        rows="4"
        class="w-full px-4 py-3 rounded bg-gray-50"
        placeholder="Describe your product..."
      ></textarea>
    </div>

    <div>
      <label for="image-url" class="block text-gray-700 mb-2">Image URL</label>
      <input
        type="url"
        id="image-url"
        name="imageUrl"
        class="w-full px-4 py-3 rounded bg-gray-50"
        placeholder="https://..."
      />
    </div>
  </div>

  <!-- Additional Options -->
  <div class="mb-8">
    <h3 class="text-lg font-medium mb-4 pb-2 border-b border-gray-100">Additional Options</h3>

    <div class="flex items-start mb-4">
      <input
        type="checkbox"
        id="perishable"
        name="is_perishable"
        class="custom-checkbox mt-1"
      />
      <label for="perishable" class="ml-3 text-gray-700">This is a perishable item</label>
    </div>

    <div class="flex items-start">
      <input type="checkbox" id="donate" class="custom-checkbox mt-1" />
      <label for="donate" class="ml-3 text-gray-700">I agree to donate unsold items to local shelters</label>
    </div>
  </div>

  <!-- 🆕 Hidden sellerName input (temp, to be automated later) -->
  <input type="hidden" name="sellerName" value="Demo Seller" />
          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row gap-4">
            <button
              type="submit"
              class="bg-primary text-white py-3 px-6 rounded-button font-medium hover:bg-primary/90 transition-colors whitespace-nowrap"
            >
              Upload Product for Sale
            </button>
            <button
              type="button"
              id="donate-btn"
              class="border-2 border-secondary text-secondary py-3 px-6 rounded-button font-medium hover:bg-secondary/10 transition-colors whitespace-nowrap"
            >
              Donate Product Now
            </button>
          </div>
        </form>
      </div>

      <!-- Footer Info -->
      <div class="text-center text-gray-500 text-sm">
        <p>
          By uploading products, you're helping reduce food waste and supporting
          your local community.
        </p>
        <p class="mt-2">© 2025 ExpiryBuddy. All rights reserved.</p>
      </div>
    </div>

    <script id="form-validation">
      document.addEventListener("DOMContentLoaded", function () {
        //const form = document.getElementById("product-form");
        const donateBtn = document.getElementById("donate-btn");

        
      //   form.addEventListener("submit", function () {
      //   const productName = document.getElementById("product-name").value;
      //   const category = document.getElementById("category").value;
      //   const price = document.getElementById("original-price").value;

      //   if (!productName || !category || !price) {
      //   alert("Please fill in all required fields...");
      //   return;
      // }

      // alert("Product successfully uploaded for sale!");
      // // DO NOT preventDefault. Let it submit
      //  });


        donateBtn.addEventListener("click", function () {
          const productName = document.getElementById("product-name").value;

          if (!productName) {
            alert("Please enter a product name before donating");
            return;
          }

          alert(
            "Thank you for your donation! Your product will be sent to local shelters.",
          );
          form.reset();
        });

        // Calculate discounted price
        const originalPrice = document.getElementById("original-price");
        const discount = document.getElementById("discount");

        [originalPrice, discount].forEach((el) => {
          el.addEventListener("input", function () {
            if (originalPrice.value && discount.value) {
              const discountedPrice = (
                (originalPrice.value * (100 - discount.value)) /
                100
              ).toFixed(2);
              // In a real app, you might display this somewhere
              console.log("Discounted price:", discountedPrice);
            }
          });
        });
      });
    </script>

    <script id="category-subcategory">
      document.addEventListener("DOMContentLoaded", function () {
        const categorySelect = document.getElementById("category");
        const subcategorySelect = document.getElementById("subcategory");

        const subcategories = {
          dairy: ["Milk & Cream", "Cheese", "Yogurt", "Butter", "Eggs"],
          bakery: ["Bread", "Pastries", "Cakes", "Cookies", "Pies"],
          produce: ["Fruits", "Vegetables", "Herbs", "Salad Mixes"],
          meat: ["Beef", "Poultry", "Pork", "Seafood", "Deli Meats"],
          pantry: ["Canned Goods", "Pasta & Rice", "Sauces", "Spices", "Oils"],
          frozen: ["Frozen Meals", "Ice Cream", "Frozen Vegetables", "Frozen Fruits"],
          beverages: ["Juices", "Soft Drinks", "Coffee & Tea", "Alcoholic Beverages"],
        };

        categorySelect.addEventListener("change", function () {
          // Clear existing options
          subcategorySelect.innerHTML =
            '<option value="" disabled selected>Select subcategory</option>';

          const category = this.value;
          if (category && subcategories[category]) {
            subcategories[category].forEach((sub) => {
              const option = document.createElement("option");
              option.value = sub.toLowerCase().replace(/\s+/g, "-");
              option.textContent = sub;
              subcategorySelect.appendChild(option);
            });
          }
        });
      });
    </script>
  </body>
</html>